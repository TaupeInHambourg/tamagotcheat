/**
 * Button Component Barrel Export
 *
 * Centralizes exports from the Button component module.
 * Provides a clean import interface for consumers.
 *
 * Usage:
 * ```tsx
 * import Button, { ButtonProps, ButtonSize, ButtonVariant } from '@/components/Button'
 * ```
 */

export { default } from './Button'
export type { ButtonProps } from './button.types'
export type { ButtonSize, ButtonVariant } from './button.styles'
