/**
 * Creatures Loading State
 *
 * Displays during page transition to creatures page.
 *
 * @module app/creatures
 */

import { PageLoader } from '@/components/common/PageLoader'

export default function CreaturesLoading (): React.ReactNode {
  return <PageLoader />
}
