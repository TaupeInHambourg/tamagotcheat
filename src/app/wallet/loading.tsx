/**
 * Wallet Loading State
 *
 * Displays during page transition to wallet page.
 *
 * @module app/wallet
 */

import { PageLoader } from '@/components/common/PageLoader'

export default function WalletLoading (): React.ReactNode {
  return <PageLoader />
}
