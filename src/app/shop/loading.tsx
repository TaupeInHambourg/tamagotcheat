/**
 * Shop Loading State
 *
 * Displays during page transition to shop page.
 *
 * @module app/shop
 */

import { PageLoader } from '@/components/common/PageLoader'

export default function ShopLoading (): React.ReactNode {
  return <PageLoader />
}
