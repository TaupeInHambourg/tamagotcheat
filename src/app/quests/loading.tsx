/**
 * Quests Loading State
 *
 * Displays during page transition to quests page.
 *
 * @module app/quests
 */

import { PageLoader } from '@/components/common/PageLoader'

export default function QuestsLoading (): React.ReactNode {
  return <PageLoader />
}
