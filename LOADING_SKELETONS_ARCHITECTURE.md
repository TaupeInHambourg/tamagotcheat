# Loading Skeletons - Architecture Visuelle

## 🏗️ Vue d'ensemble de l'architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    ROOT LAYOUT (layout.tsx)                     │
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐    │
│  │         <SkeletonThemeProvider>                       │    │
│  │  (Configuration globale du thème - autumn colors)    │    │
│  │                                                       │    │
│  │    baseColor: #FAF3E0 (autumn-cream)                 │    │
│  │    highlightColor: #FFE5D9 (autumn-peach)            │    │
│  │    borderRadius: 0.75rem                             │    │
│  │    duration: 1.5s                                    │    │
│  └───────────────────────────────────────────────────────┘    │
│                            ↓                                    │
│                     {children}                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📄 Architecture par Page

### 1️⃣ Dashboard Page
```
┌─────────────────────────────────────────────────────────┐
│  /dashboard                                             │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  loading.tsx (Suspense Boundary)                │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  <StatsCardSkeleton />                   │  │  │
│  │  │  (4 stat cards en grille)                │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  <MonsterCardSkeleton count={3} />       │  │  │
│  │  │  (Grille 1-2-3 colonnes responsive)     │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  <QuestCardSkeleton count={3} />         │  │  │
│  │  │  (3 quêtes en preview)                   │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────┘  │
│                        ↓                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │  page.tsx (Données chargées)                    │  │
│  │  ↳ <DashboardContent />                         │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2️⃣ Shop Page
```
┌─────────────────────────────────────────────────────────┐
│  /shop                                                  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  loading.tsx                                    │  │
│  │                                                 │  │
│  │  Header with filters skeleton                  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  <AccessoryCardSkeleton count={8} />     │  │  │
│  │  │  (Grille 1-2-3-4 colonnes)               │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────┘  │
│                        ↓                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │  page.tsx → <ShopClient />                      │  │
│  │  (Données statiques ACCESSORIES_CATALOG)        │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 3️⃣ Gallery Page
```
┌─────────────────────────────────────────────────────────┐
│  /gallery                                               │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  loading.tsx                                    │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  <MonsterCardSkeleton count={6} />       │  │  │
│  │  │  (Grille publique de monstres)           │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────┘  │
│                        ↓                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │  page.tsx                                       │  │
│  │  (getPublicMonsters() → Base de données)       │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux de Chargement

### Scenario 1: Navigation vers une page
```
User clicks link
        ↓
Next.js Router activates
        ↓
loading.tsx renders IMMEDIATELY
        ↓
Skeleton components display
        ↓
Server fetches data (async)
        ↓
page.tsx renders with data
        ↓
Skeleton replaced by content
```

### Scenario 2: Component-level loading (QuestsSection)
```
Component mounts
        ↓
loading = true
        ↓
<QuestCardSkeleton /> renders
        ↓
useEffect fetches data
        ↓
loading = false
        ↓
<QuestsList /> renders
```

### Scenario 3: Real-time refresh (MonsterCard)
```
MonsterCard rendered
        ↓
useMonsterPolling hook active
        ↓
Every 3 seconds:
  ↓
  isLoading = true → Overlay shown
  ↓
  Fetch monster state
  ↓
  isLoading = false → Overlay hidden
  ↓
  Card updates with new state
```

---

## 🎨 Structure d'un Skeleton Component

```tsx
┌─────────────────────────────────────────────────────────┐
│  MonsterCardSkeleton.tsx                                │
│                                                         │
│  export function MonsterCardSkeleton({ count = 1 })    │
│                                                         │
│    ┌─────────────────────────────────────────────┐    │
│    │  MonsterCardSkeletonItem()                  │    │
│    │                                             │    │
│    │    <div className='card-creature'>          │    │
│    │      <Skeleton /> (State badge)             │    │
│    │      <Skeleton /> (Image area)              │    │
│    │      <Skeleton /> (Name)                    │    │
│    │      <Skeleton /> (Date)                    │    │
│    │    </div>                                   │    │
│    └─────────────────────────────────────────────┘    │
│                                                         │
│    return Array.from({ length: count }, ...)           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Principes respectés:
- ✅ **SRP**: Une fonction = un item skeleton
- ✅ **DRY**: Réutilisation via prop `count`
- ✅ **Composition**: Utilise `<Skeleton />` de base

---

## 🧩 Hiérarchie des Composants

```
src/components/skeletons/
│
├── index.ts
│   └── Barrel exports (Clean imports)
│
├── SkeletonThemeProvider.tsx
│   ├── Wraps: react-loading-skeleton's <SkeletonTheme>
│   └── Purpose: Global theme configuration
│
├── MonsterCardSkeleton.tsx
│   ├── Depends on: <Skeleton />
│   ├── Used in: Dashboard, Gallery, Creatures pages
│   └── Props: { count?: number }
│
├── AccessoryCardSkeleton.tsx
│   ├── Depends on: <Skeleton />
│   ├── Used in: Shop page
│   └── Props: { count?: number }
│
├── QuestCardSkeleton.tsx
│   ├── Depends on: <Skeleton />
│   ├── Used in: Dashboard, Quests page, QuestsSection
│   └── Props: { count?: number }
│
└── StatsCardSkeleton.tsx
    ├── Depends on: <Skeleton />
    ├── Used in: Dashboard page
    └── Props: none (always 4 stats)
```

---

## 🔗 Dépendances

```
┌───────────────────────────────────────────────────────┐
│  Application Code                                     │
│                                                       │
│  ┌─────────────────────────────────────────────┐    │
│  │  Skeleton Components                        │    │
│  │  (MonsterCardSkeleton, etc.)                │    │
│  └─────────────────┬───────────────────────────┘    │
│                    │ imports                         │
│                    ↓                                  │
│  ┌─────────────────────────────────────────────┐    │
│  │  react-loading-skeleton                     │    │
│  │  (External Library)                         │    │
│  │                                             │    │
│  │  - <Skeleton />                             │    │
│  │  - <SkeletonTheme />                        │    │
│  │  - CSS animations                           │    │
│  └─────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────┘
```

### Inversion de dépendance (DIP)
- ✅ Application dépend d'**abstractions** (interfaces)
- ✅ Pas de couplage direct avec implémentations
- ✅ Facilite les tests et le remplacement

---

## 📊 Flux de Données

```
┌─────────────────────────────────────────────────────────┐
│  Server (Next.js)                                       │
│                                                         │
│  Data fetching                                          │
│  (getMonsters, getQuests, etc.)                         │
│                    ↓                                    │
└────────────────────┼───────────────────────────────────┘
                     │
                     │ During fetch...
                     │
┌────────────────────▼───────────────────────────────────┐
│  Client (Browser)                                       │
│                                                         │
│  Skeleton Components                                    │
│  (Visual placeholders)                                  │
│                    ↓                                    │
│  ┌──────────────────────────────────────────────┐     │
│  │  <Skeleton />                                │     │
│  │  Animated loading bars                       │     │
│  │  (CSS transform animations)                  │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  After fetch completes...                               │
│                    ↓                                    │
│  ┌──────────────────────────────────────────────┐     │
│  │  Actual Content                              │     │
│  │  (MonsterCard, AccessoryCard, etc.)          │     │
│  └──────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Points Clés

### ✅ Avantages Architecturaux

1. **Séparation des préoccupations**
   - Loading state ≠ Content state
   - Chaque composant a sa responsabilité

2. **Réutilisabilité**
   - Skeletons utilisables partout
   - Props simples pour customisation

3. **Maintenabilité**
   - Changement de structure = 1 seul fichier
   - Thème centralisé = 1 seul endroit

4. **Testabilité**
   - Composants isolés
   - Faciles à tester unitairement

5. **Performance**
   - Skeletons légers (CSS uniquement)
   - Pas d'impact sur le bundle size

---

**🎨 Architecture propre, scalable et maintenable !**
